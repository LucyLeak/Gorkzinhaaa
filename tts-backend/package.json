{
  "name": "gork-tts-backend",
  "version": "1.0.0",
  "description": "TTS delivery backend for Gork bot — polls PostgreSQL, serves audio, broadcasts via WebSocket",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
